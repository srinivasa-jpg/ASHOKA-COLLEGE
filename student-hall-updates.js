(function(){
  const YEARS=['I','II','III','IV'];
  let studentPage=1;
  const studentPageSize=50;
  const selectedStudents=new Set();

  function esc2(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));}
  function msg(t){ if(typeof toast==='function') toast(t); else alert(t); }
  function canStudent(action){return window.CampusRoles?.can?window.CampusRoles.can('students',action):true}

  function injectModuleStyles(){
    if(document.getElementById('student-hall-update-styles')) return;
    const s=document.createElement('style');s.id='student-hall-update-styles';s.textContent=`
      .student-actions{display:flex;gap:6px;align-items:center;white-space:nowrap}.student-action-btn{border:1px solid var(--line);background:var(--surface);color:var(--text);padding:6px 10px;border-radius:9px;font-size:12px;font-weight:800}.student-action-btn:hover{border-color:#60a5fa;color:var(--primary)}.student-action-btn.delete{color:var(--danger)}.student-action-btn.delete:hover{border-color:#fca5a5;background:rgba(220,38,38,.05)}
      .student-pager{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:14px 2px 0;flex-wrap:wrap}.student-pager-info{font-size:12px;color:var(--muted);font-weight:800}.student-pager-actions{display:flex;gap:7px;align-items:center}.student-page-btn{border:1px solid var(--line);background:var(--surface);color:var(--text);border-radius:9px;padding:7px 11px;font-weight:800}.student-page-btn:disabled{opacity:.45;cursor:not-allowed}.student-clear-btn{white-space:nowrap}.student-empty-filter{padding:26px;text-align:center;color:var(--muted)}
      .student-bulk-bar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:12px 0;padding:10px 12px;border:1px solid var(--line);background:var(--surface-2);border-radius:12px}.student-selected-count{font-size:12px;font-weight:900;color:var(--text);margin-right:auto}.student-row-check,.student-head-check{width:17px;height:17px;accent-color:var(--primary)}.student-select-cell{width:38px;text-align:center}.student-bulk-modal{width:min(650px,94vw)}.student-bulk-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.student-bulk-grid label{display:grid;gap:6px;font-size:12px;font-weight:800}.student-bulk-note{font-size:12px;color:var(--muted);margin-bottom:12px}
      .hall-filter-panel{border:1px solid var(--line);background:var(--surface-2);border-radius:14px;padding:12px;margin:0 0 14px}.hall-filter-title{font-size:12px;font-weight:900;margin:0 0 8px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em}.hall-filter-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.hall-filter-grid label{display:grid;gap:5px;font-size:12px;font-weight:800}.hall-filter-summary{margin:9px 0 0;font-size:12px;color:var(--muted);font-weight:700}
      @media(max-width:650px){.hall-filter-grid,.student-bulk-grid{grid-template-columns:1fr}.student-pager{align-items:flex-start;flex-direction:column}}
    `;document.head.appendChild(s);
  }

  function branchList(){const masters=Array.isArray(state?.branches)?state.branches.filter(x=>(x.status||'Active')==='Active'):[];if(masters.length) return masters.map(x=>({code:x.code,name:x.name||x.code}));const codes=[...new Set((state?.students||[]).map(x=>x.branch).filter(Boolean))].sort();return codes.map(code=>({code,name:code}));}
  function filteredStudentItems(){const q=document.getElementById('studentSearch')?.value.trim().toLowerCase()||'',branch=document.getElementById('branchFilter')?.value||'',year=document.getElementById('yearFilter')?.value||'',sem=document.getElementById('semesterFilter')?.value||'';return(state.students||[]).slice().sort((a,b)=>String(a.hall||'').localeCompare(String(b.hall||''))).filter(s=>(!q||`${s.hall} ${s.name}`.toLowerCase().includes(q))&&(!branch||s.branch===branch)&&(!year||s.year===year)&&(!sem||s.sem===sem))}

  function installStudentPagerUI(){
    const filters=document.querySelector('#students .filters');
    if(filters&&!document.getElementById('studentClearFilters')){const btn=document.createElement('button');btn.type='button';btn.className='btn btn-outline student-clear-btn';btn.id='studentClearFilters';btn.textContent='Clear Filters';const exportBtn=document.getElementById('exportStudentsBtn');filters.insertBefore(btn,exportBtn||null);btn.addEventListener('click',()=>{const q=document.getElementById('studentSearch'),b=document.getElementById('branchFilter'),y=document.getElementById('yearFilter'),s=document.getElementById('semesterFilter');if(q)q.value='';if(b)b.value='';if(y)y.value='';if(s)s.value='';studentPage=1;renderStudents();});}
    const wrap=document.querySelector('#students .table-wrap');
    if(wrap&&!document.getElementById('studentPager')){const p=document.createElement('div');p.className='student-pager';p.id='studentPager';p.innerHTML='<div class="student-pager-info" id="studentPagerInfo"></div><div class="student-pager-actions"><button class="student-page-btn" id="studentPrevPage" type="button">← Previous</button><span class="student-pager-info" id="studentPageLabel"></span><button class="student-page-btn" id="studentNextPage" type="button">Next →</button></div>';wrap.after(p);document.getElementById('studentPrevPage').addEventListener('click',()=>{if(studentPage>1){studentPage--;renderStudents();document.querySelector('#students .card')?.scrollIntoView({behavior:'smooth',block:'start'})}});document.getElementById('studentNextPage').addEventListener('click',()=>{studentPage++;renderStudents();document.querySelector('#students .card')?.scrollIntoView({behavior:'smooth',block:'start'})});}
    ['studentSearch','branchFilter','yearFilter','semesterFilter'].forEach(id=>{const el=document.getElementById(id);if(el&&!el.dataset.pageReset){el.dataset.pageReset='1';el.addEventListener(id==='studentSearch'?'input':'change',()=>{studentPage=1})}});
  }

  function ensureStudentSelectionUI(){
    const card=document.querySelector('#students .card');if(!card)return;
    const table=card.querySelector('table');if(table){const head=table.querySelector('thead tr');if(head&&!head.querySelector('.student-select-head')){const th=document.createElement('th');th.className='student-select-head student-select-cell';th.innerHTML='<input type="checkbox" id="studentPageSelectAll" class="student-head-check" title="Select visible page">';head.insertBefore(th,head.firstChild);th.querySelector('input').addEventListener('change',ev=>{document.querySelectorAll('#studentTableBody .student-row-check').forEach(ch=>{ch.checked=ev.target.checked;const id=Number(ch.dataset.id);if(ev.target.checked)selectedStudents.add(id);else selectedStudents.delete(id)});updateStudentSelectionUI()})}}
    if(!document.getElementById('studentBulkBar')){const bar=document.createElement('div');bar.id='studentBulkBar';bar.className='student-bulk-bar';bar.innerHTML='<span class="student-selected-count" id="studentSelectedCount">0 selected</span><button class="btn btn-outline" id="studentSelectFiltered" type="button">Select All Matching</button><button class="btn btn-outline" id="studentClearSelected" type="button">Clear Selection</button><button class="btn btn-outline" id="studentUpdateSelected" type="button">Update Selected</button><button class="btn btn-outline" id="studentDeleteSelected" type="button">Delete Selected</button>';const filters=card.querySelector('.filters');filters?.after(bar);document.getElementById('studentSelectFiltered').addEventListener('click',()=>{filteredStudentItems().forEach(s=>selectedStudents.add(Number(s.id)));renderStudents()});document.getElementById('studentClearSelected').addEventListener('click',()=>{selectedStudents.clear();renderStudents()});document.getElementById('studentUpdateSelected').addEventListener('click',openStudentBulkEdit);document.getElementById('studentDeleteSelected').addEventListener('click',deleteSelectedStudents)}
    const body=document.getElementById('studentTableBody');if(body&&!body.dataset.selectBound){body.dataset.selectBound='1';body.addEventListener('change',ev=>{const ch=ev.target.closest('.student-row-check');if(!ch)return;const id=Number(ch.dataset.id);if(ch.checked)selectedStudents.add(id);else selectedStudents.delete(id);updateStudentSelectionUI()})}
    applyStudentPermissionUI();
  }

  function applyStudentPermissionUI(){const bulk=canStudent('bulk'),add=canStudent('add');const bar=document.getElementById('studentBulkBar');if(bar)bar.classList.toggle('role-hidden',!bulk);document.querySelectorAll('.student-select-head,.student-select-cell').forEach(x=>x.classList.toggle('role-hidden',!bulk));const addBtn=document.getElementById('addStudentBtn');if(addBtn)addBtn.classList.toggle('role-hidden',!add);const bulkBtn=document.getElementById('bulkStudentBtn');if(bulkBtn)bulkBtn.classList.toggle('role-hidden',!bulk);const tpl=document.getElementById('downloadStudentTemplateBtn');if(tpl)tpl.classList.toggle('role-hidden',!bulk);updateStudentSelectionUI()}
  function updateStudentSelectionUI(){const n=document.getElementById('studentSelectedCount');if(n)n.textContent=`${selectedStudents.size} selected`;const u=document.getElementById('studentUpdateSelected'),d=document.getElementById('studentDeleteSelected');if(u)u.disabled=!selectedStudents.size||!canStudent('update');if(d)d.disabled=!selectedStudents.size||!canStudent('delete');const page=document.getElementById('studentPageSelectAll'),checks=[...document.querySelectorAll('#studentTableBody .student-row-check')];if(page){page.checked=!!checks.length&&checks.every(x=>x.checked);page.indeterminate=checks.some(x=>x.checked)&&!page.checked}}

  function ensureStudentBulkModal(){if(document.getElementById('studentBulkEditBackdrop'))return;const d=document.createElement('div');d.className='modal-backdrop';d.id='studentBulkEditBackdrop';d.hidden=true;d.innerHTML=`<div class="modal student-bulk-modal" role="dialog" aria-modal="true"><div class="modal-head"><div><h3>Update Selected Students</h3><p class="modal-subtitle" id="studentBulkEditCount"></p></div><button class="icon-btn" id="closeStudentBulkEdit" type="button">✕</button></div><div class="modal-body"><p class="student-bulk-note">Choose only the fields you want to change. Leave a field as “No change” to keep its current value.</p><div class="student-bulk-grid"><label>Branch<select class="input" id="studentBulkBranch"></select></label><label>Year<select class="input" id="studentBulkYear"><option value="">No change</option>${YEARS.map(x=>`<option>${x}</option>`).join('')}</select></label><label>Semester<select class="input" id="studentBulkSem"><option value="">No change</option><option>I</option><option>II</option></select></label><label>Regulation<input class="input" id="studentBulkRegulation" placeholder="Leave blank for no change"></label><label>Status<select class="input" id="studentBulkStatus"><option value="">No change</option><option>Active</option><option>Detained</option><option>Completed</option></select></label></div></div><div class="modal-actions"><button class="btn btn-outline" id="cancelStudentBulkEdit" type="button">Cancel</button><button class="btn btn-primary" id="applyStudentBulkEdit" type="button">Apply Changes</button></div></div>`;document.body.appendChild(d);const close=()=>d.hidden=true;document.getElementById('closeStudentBulkEdit').onclick=close;document.getElementById('cancelStudentBulkEdit').onclick=close;document.getElementById('applyStudentBulkEdit').onclick=applyStudentBulkEdit}
  function openStudentBulkEdit(){if(!canStudent('update'))return msg('You do not have permission to update students');if(!selectedStudents.size)return msg('Select students first');ensureStudentBulkModal();const b=document.getElementById('studentBulkBranch');b.innerHTML='<option value="">No change</option>'+branchList().map(x=>`<option value="${esc2(x.code)}">${esc2(x.code)} — ${esc2(x.name)}</option>`).join('');document.getElementById('studentBulkYear').value='';document.getElementById('studentBulkSem').value='';document.getElementById('studentBulkRegulation').value='';document.getElementById('studentBulkStatus').value='';document.getElementById('studentBulkEditCount').textContent=`${selectedStudents.size} student record${selectedStudents.size===1?'':'s'} selected`;document.getElementById('studentBulkEditBackdrop').hidden=false}
  function applyStudentBulkEdit(){const changes={branch:document.getElementById('studentBulkBranch').value,year:document.getElementById('studentBulkYear').value,sem:document.getElementById('studentBulkSem').value,regulation:document.getElementById('studentBulkRegulation').value.trim(),status:document.getElementById('studentBulkStatus').value};if(!Object.values(changes).some(Boolean))return msg('Choose at least one field to update');let count=0;state.students=(state.students||[]).map(s=>{if(!selectedStudents.has(Number(s.id)))return s;count++;const next={...s};Object.entries(changes).forEach(([k,v])=>{if(v)next[k]=v});return next});saveData();document.getElementById('studentBulkEditBackdrop').hidden=true;selectedStudents.clear();renderStudents();if(typeof populateStudentSelects==='function')populateStudentSelects();if(typeof populateHallSelects==='function')populateHallSelects();msg(`${count} student record${count===1?'':'s'} updated`)}
  function deleteSelectedStudents(){if(!canStudent('delete'))return msg('You do not have permission to delete students');if(!selectedStudents.size)return msg('Select students first');if(!confirm(`Delete ${selectedStudents.size} selected student record${selectedStudents.size===1?'':'s'}? This also removes their saved result records.`))return;const ids=new Set(selectedStudents);state.students=(state.students||[]).filter(s=>!ids.has(Number(s.id)));if(state.results)ids.forEach(id=>delete state.results[id]);saveData();const count=ids.size;selectedStudents.clear();renderStudents();if(typeof renderDashboard==='function')renderDashboard();if(typeof populateStudentSelects==='function')populateStudentSelects();if(typeof populateHallSelects==='function')populateHallSelects();msg(`${count} student record${count===1?'':'s'} deleted`)}

  function protectReset(){const btn=document.getElementById('resetDemoBtn');if(!btn)return;const total=(state.students||[]).length;if(total>10){btn.disabled=true;btn.textContent='Reset Disabled';btn.title=`Protected because ${total} student records are stored in this browser`;}}

  function installStudentActions(){
    const table=document.querySelector('#students table');if(table){const ths=table.querySelectorAll('thead th');if(ths.length) ths[ths.length-1].textContent='Actions';}
    installStudentPagerUI();ensureStudentSelectionUI();
    renderStudents=function(){installStudentPagerUI();ensureStudentSelectionUI();const all=(state.students||[]).slice().sort((a,b)=>String(a.hall||'').localeCompare(String(b.hall||''))),items=filteredStudentItems();const pages=Math.max(1,Math.ceil(items.length/studentPageSize));if(studentPage>pages)studentPage=pages;if(studentPage<1)studentPage=1;const start=(studentPage-1)*studentPageSize,end=Math.min(start+studentPageSize,items.length),pageItems=items.slice(start,end),bulk=canStudent('bulk'),up=canStudent('update'),del=canStudent('delete');const body=document.getElementById('studentTableBody');if(body){body.innerHTML=pageItems.map(s=>`<tr><td class="student-select-cell ${bulk?'':'role-hidden'}"><input class="student-row-check" type="checkbox" data-id="${s.id}" ${selectedStudents.has(Number(s.id))?'checked':''}></td><td><b>${esc2(s.hall)}</b></td><td>${esc2(s.name)}</td><td>${esc2(s.branch)}</td><td>${esc2(s.year)}</td><td>${esc2(s.sem)}</td><td>${esc2(s.regulation)}</td><td>${typeof badge==='function'?badge(s.status):esc2(s.status)}</td><td><div class="student-actions">${up?`<button class="student-action-btn" type="button" onclick="updateStudentRecord(${s.id})">Update</button>`:''}${del?`<button class="student-action-btn delete" type="button" onclick="deleteStudentRecord(${s.id})">Delete</button>`:''}${!up&&!del?'—':''}</div></td></tr>`).join('') || `<tr><td colspan="9" class="student-empty-filter">${all.length?`No students match the selected filters. Total stored students: <b>${all.length}</b>. Click <b>Clear Filters</b>.`:'No students are currently stored in this browser.'}</td></tr>`;}const c=document.getElementById('studentFilterCount');if(c)c.textContent=`${items.length} matching / ${all.length} total`;const info=document.getElementById('studentPagerInfo');if(info)info.textContent=items.length?`Showing ${start+1}–${end} of ${items.length} matching students · ${all.length} total stored`:`0 matching students · ${all.length} total stored`;const label=document.getElementById('studentPageLabel');if(label)label.textContent=`Page ${items.length?studentPage:0} of ${items.length?pages:0}`;const prev=document.getElementById('studentPrevPage'),next=document.getElementById('studentNextPage');if(prev)prev.disabled=studentPage<=1||!items.length;if(next)next.disabled=studentPage>=pages||!items.length;applyStudentPermissionUI();protectReset();};
    window.resetStudentPagination=function(){studentPage=1};
    window.updateStudentRecord=function(id){if(!canStudent('update'))return msg('You do not have permission to update students');const student=(state.students||[]).find(x=>Number(x.id)===Number(id));if(!student){msg('Student not found');return;}openStudentModal(student);};
    window.deleteStudentRecord=function(id){if(!canStudent('delete'))return msg('You do not have permission to delete students');const student=(state.students||[]).find(x=>Number(x.id)===Number(id));if(!student){msg('Student not found');return;}if(!confirm(`Delete student ${student.hall} — ${student.name}?`)) return;state.students=(state.students||[]).filter(x=>Number(x.id)!==Number(id));selectedStudents.delete(Number(id));if(state.results&&Object.prototype.hasOwnProperty.call(state.results,id))delete state.results[id];saveData();renderStudents();if(typeof renderDashboard==='function')renderDashboard();if(typeof populateStudentSelects==='function')populateStudentSelects();if(typeof populateHallSelects==='function')populateHallSelects();msg('Student deleted successfully');};
    window.refreshStudentAdminControls=function(){renderStudents()};renderStudents();
  }

  function filteredStudents(){const branch=document.getElementById('hallBranchFilter')?.value||'',year=document.getElementById('hallYearFilter')?.value||'',sem=document.getElementById('hallSemesterFilter')?.value||'';return(state.students||[]).filter(s=>(s.status||'Active')==='Active').filter(s=>(!branch||s.branch===branch)&&(!year||s.year===year)&&(!sem||s.sem===sem)).sort((a,b)=>String(a.hall).localeCompare(String(b.hall)));}
  function fillStudentSelect(sel,placeholder,students,current){if(!sel)return;sel.innerHTML=`<option value="">${placeholder}</option>`+students.map(s=>`<option value="${s.id}">${esc2(s.hall)} — ${esc2(s.name)}</option>`).join('');if(current&&students.some(s=>String(s.id)===String(current)))sel.value=String(current)}
  function refreshHallStudentFilters(){const list=filteredStudents(),s1=document.getElementById('hallStudentSelect'),s2=document.getElementById('hallStudentSelect2'),current1=s1?.value||'',current2=s2?.value||'';fillStudentSelect(s1,'Select first student',list,current1);fillStudentSelect(s2,'Select second student',list,current2);const summary=document.getElementById('hallFilterSummary');if(summary)summary.textContent=`${list.length} active student${list.length===1?'':'s'} available for the selected Branch / Year / Semester.`}

  function installHallTicketFilters(){const form=document.querySelector('#halltickets .form-grid.single');if(!form)return;if(!document.getElementById('hallBranchFilter')){const panel=document.createElement('div');panel.className='hall-filter-panel';panel.innerHTML=`<p class="hall-filter-title">Filter Students</p><div class="hall-filter-grid"><label>Branch<select class="input" id="hallBranchFilter"><option value="">All Branches</option></select></label><label>Year<select class="input" id="hallYearFilter"><option value="">All Years</option>${YEARS.map(y=>`<option value="${y}">${y} Year</option>`).join('')}</select></label><label>Semester<select class="input" id="hallSemesterFilter"><option value="">All Semesters</option><option value="I">Semester I</option><option value="II">Semester II</option></select></label></div><p class="hall-filter-summary" id="hallFilterSummary"></p>`;form.parentElement.insertBefore(panel,form);const b=document.getElementById('hallBranchFilter');b.innerHTML='<option value="">All Branches</option>'+branchList().map(x=>`<option value="${esc2(x.code)}">${esc2(x.code)} — ${esc2(x.name)}</option>`).join('');['hallBranchFilter','hallYearFilter','hallSemesterFilter'].forEach(id=>document.getElementById(id)?.addEventListener('change',refreshHallStudentFilters));}populateHallSelects=function(){const examSel=document.getElementById('hallExamSelect'),currentExam=examSel?.value||'';if(examSel){examSel.innerHTML='<option value="">Select examination</option>'+(state.exams||[]).map(e=>`<option value="${e.id}">${esc2(e.name)}</option>`).join('');if(currentExam&&(state.exams||[]).some(e=>String(e.id)===String(currentExam)))examSel.value=currentExam;}refreshHallStudentFilters();};populateHallSelects();}

  injectModuleStyles();installStudentActions();installHallTicketFilters();protectReset();
  document.querySelector('[data-view="halltickets"]')?.addEventListener('click',()=>setTimeout(()=>{const b=document.getElementById('hallBranchFilter');if(b){const current=b.value;b.innerHTML='<option value="">All Branches</option>'+branchList().map(x=>`<option value="${esc2(x.code)}">${esc2(x.code)} — ${esc2(x.name)}</option>`).join('');if([...b.options].some(o=>o.value===current))b.value=current;}refreshHallStudentFilters();},0));
  if(!document.querySelector('script[data-attendance-loader]')){const loader=document.createElement('script');loader.src='attendance-module.js';loader.dataset.attendanceLoader='true';document.body.appendChild(loader);}
})();